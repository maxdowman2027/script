# rx_iq_imbalance_psd.py

## 功能
- **1:1** 移植 Xian `myplot.psd_plot_rx_cal`
- 差异：bw/chan/freqcw 用配置变量；I/Q 从指定 CSV 读取
- **目录批量**：`INPUT_CSV` / CLI `input` 可为**目录**，自动检索 `*.csv`
- **列名可配**：`COL_I`/`COL_Q` 或 `--col-i`/`--col-q`
- **归一化可选**：`NORM_MODE` / `--norm-mode` = `adc` 或 `peak`

## 配置
```python
INPUT_CSV = r"D:\path\to\dump_dir"   # 单文件 CSV，或目录（自动扫 *.csv）
RECURSIVE = False                    # 目录模式是否递归子目录
OUTPUT_PDF = ""                      # 单文件: PDF stem；目录: 可选输出目录
BW_MHZ = 20
CH_FREQ_MHZ = 2462
FREQCW_MHZ = 2467
COL_I = "sample_i"
COL_Q = "sample_q"
NORM_MODE = "peak"   # 或 "adc"
```

## 运行示例
```bash
# 配置区 INPUT_CSV 为目录时：无参即批量
python rx_iq_imbalance_psd.py

# 显式目录 / 递归 / 输出目录
python rx_iq_imbalance_psd.py "D:\path\to\dump_dir" --norm-mode peak
python rx_iq_imbalance_psd.py "D:\path\to\dump_dir" -r -o "D:\path\to\out"

# 单文件
python rx_iq_imbalance_psd.py data.csv --bw 20 --chan 2412 --freqcw 2417 --col-i iq_cmp_i --col-q iq_cmp_q
```

## 输出
- `<stem>.pdf` / `<stem>_iq_cal_result.csv`
- 目录模式额外：`iq_cal_batch_summary.csv`（扫描时自动跳过该文件与 `*_iq_cal_result.csv`）

## 文档
`skill/rx_iq_imbalance_psd_Skill.md`
