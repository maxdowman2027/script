# mag_track_rls4_fpga_analyse.py

## 适用环境
**RLS4.0 FPGA** `mag_track_test_res_*.csv`。**E22** 用 `txmagtrk_analyse.py`。

## 作图规则（摘要）
- **横轴**：`tx_pwr`。
- **仅当**某完整分面元组（默认含 `chan,dly,start_point,...`）存在 **`tx_mag_track_on=1`** 时出 **一页 PDF**。
- **同图**：**FPGA magtrack on** `(1,0)/(1,1)` 用 **该分面 mag 元组**；**`tx_mag_track_on=0`** 基线 **仅同 `chan`**（**所有 `dly`/mag** 在 `tx_pwr` 上均值），保证每页有蓝/红对比线。

## 运行
```bash
python mag_track_rls4_fpga_analyse.py [csv_or_dir] -o <out_dir> [--group-cols ...] [--no-evm-infer]
```

## 文档与告警
- 全文说明：`skill/mag_track_rls4_fpga_analyse_Skill.md`
- 缺基线时：与 PDF 同前缀的 **`<basename>_rls4_magtrk_plot_warnings.txt`**
