# xian_static_dpd — Xian static DPD MATLAB→Python port

## 功能
读 ILA CSV（**feedback/ref** 或 **dac** 布局；可选 `--dac-csv`）→ 速率匹配（OSR）→ 包络粗对齐 → 增益 → CFO → DC → 分数时延 → LUT / `lut_data_map` / AM 图。

## 入口（常用）
```bash
cd D:\users\gxu\scripts

# DAC vs iQxel（dac_iladata.csv: adc_* + dac_*）
python dpd/xian_static_dpd_main1.py --csv PATH\dac_iladata.csv --txt PATH\iqxel.txt --tx-source dac --rx txt -o OUT

# 发送 tone：DAC 节点 vs iQxel（自动识别 CW，复数/去直流对齐 + 频谱图）
python dpd/xian_static_dpd_main1.py --csv PATH\tone\dac_iladata.csv --txt PATH\tone\iqxel_2412_tone.txt --tx-source dac --rx txt -o OUT
# 可强制：--signal-mode tone  --fs 80e6

# ref CSV vs dac CSV（双文件；--dac-csv 用全局包络对齐）
python dpd/xian_static_dpd_main1.py --csv PATH\feedback_ref_iladata.csv --tx-source ref --dac-csv PATH\dac_iladata.csv --rx dac -o OUT

# ref(4x ILA) vs pkt_out dac（模块名 2x，ILA 同采样钟 → 默认两侧 decimate=2 + 全局对齐）
python dpd/xian_static_dpd_main1.py --csv PATH\feedback_ref_iladata.csv --tx-source ref --dac-csv PATH\pkt_out_iladata.csv --rx dac -o OUT

# 真·半速率 dump 与 4x：显式 OSR 重采样到 work_osr
python dpd/xian_static_dpd_main1.py --csv PATH\ref.csv --csv-osr 4 --dac-csv PATH\pkt.csv --dac-csv-osr 2 --work-osr 2 --tx-source ref --rx dac -o OUT

# ref vs iQxel（全局自动找 RX 起点）
python dpd/xian_static_dpd_main1.py --csv PATH\iladata.csv --txt PATH\iqxel.txt --rx txt --tx-source ref -o OUT

# ILA 片上：ref vs feedback
python dpd/xian_static_dpd_main1.py --csv PATH\iladata.csv --rx feedback --tx-source ref -o OUT
```

## TX / RX / 速率
| 参数 | 取值 |
|------|------|
| `--tx-source` | `ref` / `dac` / `adc` / `auto`（能量：ref→dac→adc） |
| `--rx` | `txt` / `mat` / `feedback` / `dac` / `csv` / `auto` |
| `--dac-csv` | 第二份含 `dac_i/q` 的 CSV（配合 `--rx dac`，**全局**包络对齐） |
| `--csv-osr` / `--dac-csv-osr` / `--work-osr` | 过采样标注与工作速率（可选；真半速率时用） |
| `--csv-decimate` / `--dac-csv-decimate` | 行抽稀（默认 2；设 OSR 时忽略） |

CSV 按表头自动识别：`feedback_ref` 或 `dac`。

## 时域图（增益前）
`pre_gain_time_domain` / `align_time_domain`：\|·\|、peak-norm、I、Q。  
I/Q 叠画相位用 LS 复数增益 `a/|a|`（`a=⟨rx,tx⟩/⟨rx,rx⟩`）。  
旧版 `exp(-j∠g)` 符号错误会在 ~90° 相位差时把 I/Q **画成整体取反**；已修复。真 180° 载波差由 `gain_compensation` 吸收，属正常。

## 文档
`skill/xian_static_dpd_Skill.md`
