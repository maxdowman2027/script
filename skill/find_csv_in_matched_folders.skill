# find_csv_in_matched_folders.py

## 配置区（脚本顶部，优先改这里）
| 变量 | 作用 |
|------|------|
| `ROOT_SEARCH_PATH` | 递归检索根目录 |
| `FOLDER_PATTERN` | 文件夹名通配符，如 `wifi_txrx_test_RXSens_*_mld_en0_cur_degree0` |
| `CSV_PATTERN` | CSV 文件名，默认 `*.csv` |
| `MOVE_DIR` / `COPY_DIR` | 移动或复制目标（二选一，`None` 表示仅检索） |

改完后：`python find_csv_in_matched_folders.py`  
CLI（`-r`、`-f`、`--move-dir`）可覆盖配置。

## 运行示例
```bash
python find_csv_in_matched_folders.py
python find_csv_in_matched_folders.py --move-dir "D:\other\out"
```

## 文档
`skill/find_csv_in_matched_folders_Skill.md`
