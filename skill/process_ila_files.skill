# ILA信号文件处理脚本 - process_ila_files.py

## 📖 脚本概述

这是一个专门用于处理 FPGA 导出的 ILA（Integrated Logic Analyzer）信号文件的 Python 脚本。它能够自动识别并解压缩 `.ila` 文件（实际上是 ZIP 压缩包格式），并将其中的 `waveform.csv` 文件按照原始文件名进行更名。

## 🎯 核心功能

### 主要特性
- **自动识别ILA文件格式**：智能识别并处理 FPGA 导出的 `.ila` 文件（ZIP 压缩包格式）
- **提取waveform.csv**：自动提取每个 ILA 文件中的 `waveform.csv` 文件
- **Vivado 新旧格式兼容**：自动识别并剔除 Vivado 2025+ 在表头下插入的 `Radix - UNSIGNED,...,HEX,...` 元数据行；旧版无该行时保持原样
- **智能重命名**：按照原始 ILA 文件名自动重命名提取的 CSV 文件
- **列筛选**：提取后可只保留指定列
- **列重命名**：提取后可按映射关系重命名列名
- **仅改列名（passthrough）**：保留指定列单元格原样，只改表头；跳过有符号位域转换
- **位域有符号提取**：从指定列的 `[high:low]` 位域提取数据，转为有符号十进制
- **批量处理**：支持同时处理多个 ILA 文件
- **进度显示**：提供详细的处理进度和状态信息
- **容错处理**：完善的错误处理和容错机制

### 处理流程
1. 遍历指定目录下的所有 `.ila` 文件
2. 检查文件格式有效性
3. 解压缩 ILA 文件
4. 提取 `waveform.csv` 文件
5. **剔除 Radix 元数据行**（若存在；Vivado 2025+）
6. 按照原始文件名重命名 CSV 文件
7. 可选：保留列 → 位域有符号提取（跳过 passthrough 列）→ passthrough 只改列名 → 普通列重命名
8. 保存到指定输出目录

### Vivado waveform.csv 行格式差异

| 版本 | 行结构 |
|------|--------|
| 旧版 Vivado | `header` → `sample 0` → `sample 1` → … |
| Vivado 2025+ | `header` → `Radix - UNSIGNED,...,HEX,...` → `sample 0` → … |

若不剔除 Radix 行，位域有符号转换会对单元格 `"hex"` 执行 `int(..., 16)`，报错：
`invalid literal for int() with base 16: 'hex'`。

脚本通过 `is_ila_radix_metadata_row` / `read_ila_waveform_csv` /
`write_ila_waveform_csv_without_radix` 自动处理；`parse_ila_cell_int` 对 radix
标签也会给出明确错误，避免静默误解析。

## 🛠️ 使用方法

### 1. 代码配置方式（推荐）

在脚本顶部的配置区域修改参数：

```python
# ===========================================
# 配置参数 - 可根据需要修改
# ===========================================
# 输入目录：包含.ila文件的目录
INPUT_DIR = r"D:\test_data\rls4\260428\tx_data2"

# 输出目录：用于保存提取后的CSV文件（默认与输入目录相同）
# 如果需要保存到其他目录，请修改此处
OUTPUT_DIR = None  # 设为None表示与输入目录相同

# 是否递归处理子目录
RECURSIVE = False

# 是否保留原始ILA文件（处理后不删除）
KEEP_ORIGINAL = True

# 是否处理后删除原始ILA文件
DELETE_ORIGINAL = False

# 提取后的CSV保留列（None表示保留全部列）
KEEP_COLUMNS = None

# 提取后的CSV列重命名映射（None表示不重命名）
# 支持以下格式：
# 1) 字典：{"old_col": "new_col"}
# 2) 字符串："old1:new1,old2:new2"
# 3) 列表：
#    - ["old1:new1", "old2:new2"]
#    - [("old1","new1"), ("old2","new2")]
#    - ["new1","new2"]  # 当KEEP_COLUMNS存在且等长时，按顺序映射
RENAME_COLUMNS = None

# 仅改列名、不改单元格（跳过 AUTO_SIGNED / SIGNED_BIT_FIELDS）
# 格式同 RENAME_COLUMNS；新列名列表短于 KEEP_COLUMNS 时，与 KEEP 尾部按序 zip
# PASSTHROUGH_RENAME = {
#     "long/path/rx_i_fe[9:0]": "adc_i",
#     "long/path/rx_q_fe[9:0]": "adc_q",
# }
PASSTHROUGH_RENAME = None

# 指定位域 → 有符号十进制（在 keep 之后、rename 之前对源列名操作）
# True：列名含 [high:low] 时自动按该位宽做有符号转换（如 adc_q_ch0[11:0]）
# PASSTHROUGH_RENAME 中的源列一律不参与自动/显式位域转换
AUTO_SIGNED_BIT_FIELDS = True

# 显式位域提取；每项 dict: column, bits(可选), output(可选)
# bits 省略时从 column 名 [high:low] 解析；同一列可拆多个位域
# SIGNED_BIT_FIELDS = [
#     {"column": "adc_q_ch0[11:0]", "output": "adc_q_ch0"},
#     {"column": "wide_bus", "bits": "[23:12]", "output": "wide_hi"},
#     {"column": "wide_bus", "bits": "[11:0]", "output": "wide_lo"},
# ]
SIGNED_BIT_FIELDS = None
```

### 2. 命令行方式

#### 基本使用
```bash
python process_ila_files.py
```

使用默认配置（代码中设定的参数）处理文件。

#### 命令行参数
```bash
python process_ila_files.py [输入目录] [选项]
```

**位置参数：**
- `输入目录`：包含.ila文件的目录（可选，默认使用代码中配置的INPUT_DIR）

**可选参数：**
- `-h, --help`：显示帮助信息
- `-o, --output_dir`：输出目录，用于保存提取后的CSV文件
- `-r, --recursive`：递归处理子目录
- `-k, --keep`：保留原始ILA文件
- `-d, --delete`：处理后删除原始ILA文件
- `--keep_columns`：提取后仅保留指定列（逗号分隔）
- `--rename_columns`：提取后重命名列（`old:new`，逗号分隔多个）
- `--passthrough-rename`：仅改列名、不改数据，且跳过有符号位域（`old:new`，逗号分隔）
- `--auto-signed`：列名含 `[high:low]` 时自动提取该位域并转为有符号十进制
- `--signed-bit-fields`：显式**有符号**位域提取，格式 `col:[hi:lo]:out` 或 `col::out`（位域从列名解析），逗号分隔多个
- `--unsigned-bit-fields`：显式**无符号**位域提取，格式同上

#### 使用示例

使用默认配置：
```bash
python process_ila_files.py
```

指定输入和输出目录：
```bash
python process_ila_files.py "D:\test_data\rls4\260428\tx_data2" -o "D:\test_data\rls4\260428\tx_data2\csv_output"
```

递归处理子目录：
```bash
python process_ila_files.py "D:\test_data\rls4" -r -o "D:\test_data\rls4\processed"
```

仅保留指定列：
```bash
python process_ila_files.py "D:\test_data\rls4\260428\tx_data2" --keep_columns "Time,sample_i,sample_q"
```

保留列并重命名：
```bash
python process_ila_files.py "D:\test_data\rls4\260428\tx_data2" --keep_columns "sample_i,sample_q" --rename_columns "sample_i:i_data,sample_q:q_data"
```

代码配置方式（`KEEP_COLUMNS` + 新列名列表）：
```python
KEEP_COLUMNS = ["dac_i_ch0[11:0]", "dac_q_ch0[11:0]"]
RENAME_COLUMNS = ["dac_i_ch0", "dac_q_ch0"]
```
上述配置会按顺序映射为：
`dac_i_ch0[11:0] -> dac_i_ch0`，`dac_q_ch0[11:0] -> dac_q_ch0`

#### 仅改列名、不改数据（PASSTHROUGH_RENAME）

适用于：列本身已是完整采样值，只想缩短表头，**不要**做有符号位域转换（避免 `AUTO_SIGNED` 改写单元格）。

```python
KEEP_COLUMNS = [
    "phy_dump_data_0d[63:0]",
    "fpga_modem_mimo_ch1_rx_i_fe[9:0]",
    "fpga_modem_mimo_ch1_rx_q_fe[9:0]",
]
# dump 走 SIGNED_BIT_FIELDS；I/Q 只改名
PASSTHROUGH_RENAME = {
    "fpga_modem_mimo_ch1_rx_i_fe[9:0]": "adc_i",
    "fpga_modem_mimo_ch1_rx_q_fe[9:0]": "adc_q",
}
# 等价短列表写法（与 KEEP 尾部 zip）：
# PASSTHROUGH_RENAME = ["adc_i", "adc_q"]
SIGNED_BIT_FIELDS = [
    {"column": "phy_dump_data_0d[63:0]", "bits": "[43:32]", "output": "feedback_i"},
    # ...
]
AUTO_SIGNED_BIT_FIELDS = True  # passthrough 源列仍会被跳过
```

命令行：
```bash
python process_ila_files.py "D:\path\to\ila_dir" \
  --keep_columns "col_a[11:0],col_b[9:0]" \
  --passthrough-rename "col_b[9:0]:adc_i" \
  --auto-signed
```

处理顺序：保留列 → 位域提取（跳过 passthrough 源列）→ passthrough 改名 → `RENAME_COLUMNS`。

#### 位域有符号提取

ILA `waveform.csv` 中信号列常为 `信号名[high:low]`，单元格为无符号十六进制或十进制。脚本可提取指定位域并按二补码转为有符号十进制。

**自动模式**（`AUTO_SIGNED_BIT_FIELDS = True` 或 `--auto-signed`）：
- 对保留列中列名带 `[11:0]` 等后缀的列，按后缀位宽提取并转有符号数
- 若列在 `PASSTHROUGH_RENAME` 中，则跳过自动转换，只改列名
- 若同时配置了 `RENAME_COLUMNS`，输出列名使用重命名映射（源列在提取后删除）

**显式有符号模式**（`SIGNED_BIT_FIELDS` 或 `--signed-bit-fields`）：
- 适用于列名无位域后缀的总线列（如 `wide_bus`），或需从同一列拆出多个子域
- 同一源列可配置多条规则（如 `[23:12]` 与 `[11:0]`）；全部提取完成后再删除源列
- 输出为**有符号**十进制（二补码）

**显式无符号模式**（`UNSIGNED_BIT_FIELDS` 或 `--unsigned-bit-fields`）：
- 格式与有符号相同（`column` / `bits` / `output`）
- 输出为**无符号**十进制（1-bit → `0/1`，`[31:0]` → `0..2³²−1`）
- 可与 `SIGNED_BIT_FIELDS` 同时使用（同一源列可同时拆有符号与无符号子域）

**自动 + 显式并用**：显式规则优先；未在 SIGNED/UNSIGNED 中声明的 `[high:low]` 列仍走自动有符号模式；均不处理 `PASSTHROUGH_RENAME` 源列。
```python
SIGNED_BIT_FIELDS = [
    {"column": "adc_q_ch0[11:0]", "output": "adc_q_ch0"},
    {"column": "wide_bus", "bits": "[23:12]", "output": "wide_hi"},
    {"column": "wide_bus", "bits": "[11:0]", "output": "wide_lo"},
]
UNSIGNED_BIT_FIELDS = [
    {"column": "phy_dump_data_0d[63:0]", "bits": "[33:33]", "output": "valid"},
    {"column": "phy_dump_data_0d[63:0]", "bits": "[31:0]", "output": "data"},
]
```

命令行等价：
```bash
python process_ila_files.py "D:\path\to\ila_dir" \
  --keep_columns "adc_q_ch0[11:0],adc_i_ch0[11:0]" \
  --rename_columns "adc_q_ch0,adc_i_ch0" \
  --auto-signed
```

或显式指定位域：
```bash
# 有符号
python process_ila_files.py "D:\path\to\ila_dir" \
  --signed-bit-fields "wide_bus:[23:12]:hi,wide_bus:[11:0]:lo"

# 无符号（valid / data 等）
python process_ila_files.py "D:\path\to\ila_dir" \
  --unsigned-bit-fields "phy_dump_data_0d[63:0]:[33:33]:valid,phy_dump_data_0d[63:0]:[31:0]:data"
```

**数值解析规则**：
- 含 `a-f` 或 `0x` 前缀 → 十六进制
- 纯数字且长度 ≤ 位宽对应十六进制位数（如 12-bit 最多 3 位）→ 按十六进制解析（`800` → 0x800 → 有符号 −2048）
- 否则按十进制解析

**转换示例**（12-bit `[11:0]`）：
| 单元格 | 有符号输出 |
|--------|------------|
| `fff`  | −1         |
| `001`  | 1          |
| `800`  | −2048      |

**实现要点**：
- 处理顺序：保留列 → 位域有符号提取（跳过 passthrough）→ passthrough 改名 → `RENAME_COLUMNS`
- 多段位域共享源列解析位宽（取各段 `high+1` 与列后缀位宽的最大值），保证宽总线十六进制字符串解析一致
- 输出为有符号十进制整数列，源列（或已拆分的总线列）在生成新列后自动删除
- `PASSTHROUGH_RENAME` 列保留原始单元格文本/数值，仅 `df.rename` 表头

**典型 magtrk / ADC 配置**（脚本顶部默认值可参考）：
```python
KEEP_COLUMNS = [
    "adc_q_ch0[11:0]", "adc_i_ch0[11:0]",
    "rxgain_i_ch0[11:0]", "rxgain_q_ch0[11:0]",
]
RENAME_COLUMNS = ["adc_q_ch0", "adc_i_ch0", "rxgain_i_ch0", "rxgain_q_ch0"]
AUTO_SIGNED_BIT_FIELDS = True
```

## 📊 输出结果

### 处理过程输出
```
[INFO] 找到 9 个.ila文件
处理进度: 100%|██████████████████| 9/9 [00:00<00:00, 91.01it/s]
[SUCCESS] 处理完成: vht_5180_pwr0_mcs7_bcc_bw20m_dacdata.ila -> vht_5180_pwr0_mcs7_bcc_bw20m_dacdata.csv
[SUCCESS] 处理完成: vht_5180_pwr0_mcs8_bcc_bw20m_dacdata.ila -> vht_5180_pwr0_mcs8_bcc_bw20m_dacdata.csv
...
[INFO] 处理完成: 成功 9 个, 失败 0 个
[INFO] 输出目录: D:\test_data\rls4\260428\tx_data2
```

### 日志颜色说明
- `[SUCCESS]`：终端显示为绿色
- `[ERROR]`：终端显示为红色
- 当输出被重定向到文件时，自动降级为纯文本（不写入ANSI颜色码）

### 生成的文件
- **原始ILA文件**：`vht_5180_pwr0_mcs7_bcc_bw20m_dacdata.ila`
- **提取后的CSV文件**：`vht_5180_pwr0_mcs7_bcc_bw20m_dacdata.csv`

## 🔧 技术参数

### 系统要求
- Python 3.6 及以上版本
- 需要安装的依赖包：
  - `tqdm`（用于进度显示）
  - `pandas`（列筛选、位域提取）

### 安装依赖
```bash
pip install tqdm pandas
```

### 支持的文件格式
- **输入文件**：`.ila` 文件（实际上是 ZIP 压缩包格式）
- **输出文件**：`.csv` 文件（提取后的 waveform.csv）

## 📁 应用场景

### 典型用途
- FPGA 测试数据处理
- ILA 信号分析
- 自动化测试报告生成
- 批量数据处理

### 适用领域
- 无线通信测试（WiFi, 蓝牙, 5G）
- 数字信号处理
- FPGA 开发和调试
- 硬件验证和测试

## 🚀 性能特点

- **处理速度快**：高效的文件处理算法
- **内存占用低**：优化的内存管理
- **并发处理**：单线程处理确保稳定性和可预测性
- **可扩展性**：模块化设计，易于添加新功能

## 📝 注意事项

### 重要提示
1. 确保输入目录路径正确，且包含有效的 `.ila` 文件
2. 输出目录需要有足够的写入权限
3. 如果选择删除原始文件，请确保已做好数据备份
4. 对于大量文件处理，建议分批处理以避免系统资源耗尽

### 常见问题
**Q: 脚本无法找到.ila文件**
A: 检查输入目录路径是否正确，或使用 `-r` 参数递归搜索子目录。

**Q: 提取的CSV文件为空或损坏**
A: 检查原始 ILA 文件是否完整，或尝试重新导出 ILA 信号。

**Q: 处理过程中出现权限错误**
A: 确保脚本有足够的权限读取输入文件和写入输出目录。

**Q: 位域转换结果不对（如 `800` 变成 800 而不是 −2048）**
A: 确认已开启 `AUTO_SIGNED_BIT_FIELDS` 或 `--auto-signed`；检查列名是否含正确的 `[high:low]` 后缀。

**Q: 宽总线拆成 hi/lo 后低位错误**
A: 使用 `SIGNED_BIT_FIELDS` 显式声明所有子域；脚本会按最大位宽统一解析源单元格，勿只对单列配置部分位域。

**Q: `001800` 被解析成十进制 1800**
A: 位域模式已强制字符串读入；若仍异常，检查是否在脚本外手工用 `read_csv` 未指定 `dtype=str` 二次处理。

**Q: 只想改列名、不要有符号转换**
A: 使用 `PASSTHROUGH_RENAME`（或 `--passthrough-rename`）；不要把这些列放进 `SIGNED_BIT_FIELDS`。即使 `AUTO_SIGNED_BIT_FIELDS=True`，passthrough 源列也会被跳过。

## 🔄 更新日志

### v1.0.0 (2024-04-30)
- 初始版本发布
- 支持基本的 ILA 文件处理功能
- 实现 waveform.csv 自动提取和重命名
- 提供命令行接口和代码配置两种使用方式

### v1.1.0 (2026-05-08)
- 新增提取后 CSV 列筛选功能（只保留指定列）
- 新增提取后 CSV 列重命名功能（支持 `old:new` 映射）
- 新增 `--keep_columns` 与 `--rename_columns` 命令行参数

### v1.1.1 (2026-05-08)
- `RENAME_COLUMNS` 增强：支持字典、`old:new` 字符串、元组列表，以及与 `KEEP_COLUMNS` 等长的“新列名列表”写法
- 增加格式兼容与错误防护，避免因配置类型不符导致运行时报错（如 list 调用 `.items()`）

### v1.2.0 (2026-05-08)
- 新增日志颜色输出：`[SUCCESS]` 绿色，`[ERROR]` 红色
- 保持重定向场景兼容性（非TTY输出保持纯文本）

### v1.3.0 (2026-07-07)
- 新增指定位域有符号提取：`AUTO_SIGNED_BIT_FIELDS`、`SIGNED_BIT_FIELDS`
- 支持 `--auto-signed`、`--signed-bit-fields`、`--unsigned-bit-fields` 命令行参数
- 配置区 `SIGNED_BIT_FIELDS` / `UNSIGNED_BIT_FIELDS` 可分别选择有符号 / 无符号位域转换
- ILA 单元格按位宽启发式解析十六进制（如 12-bit 的 `800` → −2048）
- 处理顺序：保留列 → 位域提取 → 列重命名
- 同一总线多段位域合并解析位宽；`dtype=str` 保留前导零
- 自动与显式位域规则可并存（显式优先、其余列自动推断）

### v1.4.0 (2026-07-14)
- 新增 `PASSTHROUGH_RENAME` / `--passthrough-rename`：保留指定列单元格原样，只改列名
- passthrough 源列跳过 `AUTO_SIGNED` / `SIGNED_BIT_FIELDS`；若显式位域指向这些列会 warning 并跳过
- 处理顺序：保留列 → 位域提取 → passthrough 改名 → `RENAME_COLUMNS`
- 短新列名列表可与 `KEEP_COLUMNS` 尾部 zip，便于 dump 走位域、I/Q 只改名的混合配置
- `RENAME_COLUMNS` / passthrough 的 `old:new` 解析改为 `rsplit(':', 1)`，兼容列名中的 `[high:low]`

---

**脚本作者**：WiFi Test Expert
**创建时间**：2024-04-30
**版本**：v1.4.0
**语言**：Python 3.x
