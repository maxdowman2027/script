# ILA信号文件处理脚本 - process_ila_files.py

## 📖 脚本概述

这是一个专门用于处理 FPGA 导出的 ILA（Integrated Logic Analyzer）信号文件的 Python 脚本。它能够自动识别并解压缩 `.ila` 文件（实际上是 ZIP 压缩包格式），并将其中的 `waveform.csv` 文件按照原始文件名进行更名。

## 🎯 核心功能

### 主要特性
- **自动识别ILA文件格式**：智能识别并处理 FPGA 导出的 `.ila` 文件（ZIP 压缩包格式）
- **提取waveform.csv**：自动提取每个 ILA 文件中的 `waveform.csv` 文件
- **智能重命名**：按照原始 ILA 文件名自动重命名提取的 CSV 文件
- **批量处理**：支持同时处理多个 ILA 文件
- **进度显示**：提供详细的处理进度和状态信息
- **容错处理**：完善的错误处理和容错机制

### 处理流程
1. 遍历指定目录下的所有 `.ila` 文件
2. 检查文件格式有效性
3. 解压缩 ILA 文件
4. 提取 `waveform.csv` 文件
5. 按照原始文件名重命名 CSV 文件
6. 保存到指定输出目录

## 🛠️ 使用方法

### 1. 代码配置方式（推荐）

在脚本顶部的配置区域修改参数：

```python
# ===========================================
# 配置参数 - 可根据需要修改
# ===========================================
# 输入目录：包含.ila文件的目录
INPUT_DIR = r"D:\test_data\rls4\260428\tx_data2"

# 输出目录：用于保存提取后的CSV文件（默认与输入目录相同）
# 如果需要保存到其他目录，请修改此处
OUTPUT_DIR = None  # 设为None表示与输入目录相同

# 是否递归处理子目录
RECURSIVE = False

# 是否保留原始ILA文件（处理后不删除）
KEEP_ORIGINAL = True

# 是否处理后删除原始ILA文件
DELETE_ORIGINAL = False
```

### 2. 命令行方式

#### 基本使用
```bash
python process_ila_files.py
```

使用默认配置（代码中设定的参数）处理文件。

#### 命令行参数
```bash
python process_ila_files.py [输入目录] [选项]
```

**位置参数：**
- `输入目录`：包含.ila文件的目录（可选，默认使用代码中配置的INPUT_DIR）

**可选参数：**
- `-h, --help`：显示帮助信息
- `-o, --output_dir`：输出目录，用于保存提取后的CSV文件
- `-r, --recursive`：递归处理子目录
- `-k, --keep`：保留原始ILA文件
- `-d, --delete`：处理后删除原始ILA文件

#### 使用示例

使用默认配置：
```bash
python process_ila_files.py
```

指定输入和输出目录：
```bash
python process_ila_files.py "D:\test_data\rls4\260428\tx_data2" -o "D:\test_data\rls4\260428\tx_data2\csv_output"
```

递归处理子目录：
```bash
python process_ila_files.py "D:\test_data\rls4" -r -o "D:\test_data\rls4\processed"
```

## 📊 输出结果

### 处理过程输出
```
[INFO] 找到 9 个.ila文件
处理进度: 100%|██████████████████| 9/9 [00:00<00:00, 91.01it/s]
[SUCCESS] 处理完成: vht_5180_pwr0_mcs7_bcc_bw20m_dacdata.ila -> vht_5180_pwr0_mcs7_bcc_bw20m_dacdata.csv
[SUCCESS] 处理完成: vht_5180_pwr0_mcs8_bcc_bw20m_dacdata.ila -> vht_5180_pwr0_mcs8_bcc_bw20m_dacdata.csv
...
[INFO] 处理完成: 成功 9 个, 失败 0 个
[INFO] 输出目录: D:\test_data\rls4\260428\tx_data2
```

### 生成的文件
- **原始ILA文件**：`vht_5180_pwr0_mcs7_bcc_bw20m_dacdata.ila`
- **提取后的CSV文件**：`vht_5180_pwr0_mcs7_bcc_bw20m_dacdata.csv`

## 🔧 技术参数

### 系统要求
- Python 3.6 及以上版本
- 需要安装的依赖包：
  - `tqdm`（用于进度显示）

### 安装依赖
```bash
pip install tqdm
```

### 支持的文件格式
- **输入文件**：`.ila` 文件（实际上是 ZIP 压缩包格式）
- **输出文件**：`.csv` 文件（提取后的 waveform.csv）

## 📁 应用场景

### 典型用途
- FPGA 测试数据处理
- ILA 信号分析
- 自动化测试报告生成
- 批量数据处理

### 适用领域
- 无线通信测试（WiFi, 蓝牙, 5G）
- 数字信号处理
- FPGA 开发和调试
- 硬件验证和测试

## 🚀 性能特点

- **处理速度快**：高效的文件处理算法
- **内存占用低**：优化的内存管理
- **并发处理**：单线程处理确保稳定性和可预测性
- **可扩展性**：模块化设计，易于添加新功能

## 📝 注意事项

### 重要提示
1. 确保输入目录路径正确，且包含有效的 `.ila` 文件
2. 输出目录需要有足够的写入权限
3. 如果选择删除原始文件，请确保已做好数据备份
4. 对于大量文件处理，建议分批处理以避免系统资源耗尽

### 常见问题
**Q: 脚本无法找到.ila文件**
A: 检查输入目录路径是否正确，或使用 `-r` 参数递归搜索子目录。

**Q: 提取的CSV文件为空或损坏**
A: 检查原始 ILA 文件是否完整，或尝试重新导出 ILA 信号。

**Q: 处理过程中出现权限错误**
A: 确保脚本有足够的权限读取输入文件和写入输出目录。

## 🔄 更新日志

### v1.0.0 (2024-04-30)
- 初始版本发布
- 支持基本的 ILA 文件处理功能
- 实现 waveform.csv 自动提取和重命名
- 提供命令行接口和代码配置两种使用方式

---

**脚本作者**：WiFi Test Expert
**创建时间**：2024-04-30
**版本**：v1.0.0
**语言**：Python 3.x
