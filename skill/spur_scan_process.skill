# spur_scan_process.py（spur_notch/）

## 位置
`spur_notch/spur_scan_process.py` — 仓库根执行：`python spur_notch/spur_scan_process.py`

## 三步流水线
1. IQ CSV → PSD 杂散检测 + 非DC/非40M倍频>40M倍频功率检查 → `result/spur_scan_result.csv` + `output/spectrum/*.pdf`
2. 陷波 IIR 系数定点化 → `result/spur_scan_result_coef.csv`
3. 按 Used_Frequency 算频点功率 → 回写 coef 的 `pwr`（`.bak` 备份）

## 输入
- 文件名含 `phy_mode(\d+)`、`chan(\d+)`
- 列：` sample i_ch0`、` sample q_ch0`（前导空格）

## 底部配置
`INPUT_DIR` / `OUTPUT_DIR` / `FS`(160) / `SPUR_THR`(18) / `Q`(5) / `RF_GAIN`(98) / `NFFT`(16000) / `SAVE_SPECTRUM_PLOTS`(True)

## 同源脚本
内嵌 notch_cal 思路；参考根目录 `psd_plot.py`、`clac_pwr_for_ofdm_signal.py`；独立系数见 `spur_notch/notch_cal.py`

## 文档
`skill/spur_scan_process_Skill.md`
