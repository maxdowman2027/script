# plot_psd_2462.py

## 功能
- 输入 I/Q CSV，输出两页 PDF（时域 + Welch PSD），默认 `<input_stem>_spec.pdf`
- **single**：`sample_i` / `sample_q`（单路；Ch0/Ch1 子图相同）
- **2ant**：`ch0_sample_q/i`、`ch1_sample_q/i`（双天线）
- **auto**（默认）：按列名自动选择 single / 2ant
- **PSD 与 `psd_plot.py` 对齐**：Fs 以 MHz 传入 welch，`NFFT=Fs/0.1`，`20*log10(abs(P))`
- **2抽1 抽取**：`--decimate 2` 仅用于全速率 CSV 刻意抽取；默认 `DECIMATE_FACTOR=1`
- **上采样**：`--upsample 2`，用于硬件 **已 2抽1** 的 CSV；`--fs` 填原始 ADC 采样率

## 运行
```bash
# 全速率 80MHz ADC，20MHz 带宽（勿误开 decimate）
python plot_psd_2462.py data.csv --no-decimate --no-upsample --fs 80e6 --bit-width 10

# 160MHz ADC 全速率
python plot_psd_2462.py data.csv --no-decimate --fs 160e6 --bit-width 12

# 全速率 CSV，软件 2抽1 后绘图（80MHz → 有效 40MHz）
python plot_psd_2462.py data.csv --decimate 2 --no-upsample --fs 80e6

# 硬件已 2抽1 的 CSV，上采样恢复点数
python plot_psd_2462.py input_iq8.csv --mode 2ant --bit-width 8 --fs 80e6 --upsample 2
```

## 频谱减半排查
- 误开 `--decimate 2` 或 `--fs` 与 CSV 实际采样率不符 → 见 `plot_psd_2462_Skill.md`

## 上游
`*_2ant_iq.csv` / `*_iq8.csv` / `*_iq_merged.csv` / `parse_60bit_40bit_2ant_iq.py` / `dump_64bit_to_iq8.py`

## 文档
`skill/plot_psd_2462_Skill.md`
