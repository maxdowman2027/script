# lut_phase0_fit — multi-LUT phase-through-0 fit

## 功能
默认 **`poly`**：**幅度**无常数项多项式；**相位保留训练趋势**（仅坏点插值）。  
`--master-lut` 时 slave 默认 **passthrough**（保护记忆支路）。勿用 maxerr 评判。

备选：`poly_ph`（旧 amp+phase 双 poly）、`smooth` / `repair` / `iqpoly` / `ma`。

## 入口
```bash
cd D:\users\gxu\scripts
python dpd/lut_phase0_fit.py DIR -o OUT --master-lut 0 --scope all
python dpd/fit_lut_custom_phase0.py
python dpd/lut_phase0_fit.py DIR -o OUT --method poly_ph --master-lut 0
```

## 文档
`skill/lut_phase0_fit_Skill.md`
