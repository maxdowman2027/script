# evm_comparison.py 脚本说明文档

## 脚本概述

evm_comparison.py 是一个专门用于对比两个 Wi-Fi TX 测试文件中 EVM（误差向量幅度）随发射功率变化的 Python 脚本。该脚本主要用于评估 Mag Tracking（幅度跟踪）功能开启和关闭时对 EVM 性能的影响，支持按每个 MCS（调制与编码方案）进行详细比较。

## 主要功能

### 1. 文件读取
- 读取两个特定的 Wi-Fi TX 测试文件
- 验证文件是否包含必要的列（'tx_power_set(dBm)'、'evm_nss0'、'evm_nss1' 和 'rate'）

### 2. 数据处理
- 过滤无效数据
- 使用数据透视表按发射功率和 MCS 计算每个空间流的平均 EVM（evm_nss0 和 evm_nss1 分别计算）
- 格式化图表坐标轴范围
- 识别所有唯一的 MCS（调制与编码方案）
- 新增 psdu_crc 列处理，支持显示两个版本的 psdu_crc 结果
- 对 psdu_crc 为 Fail 的单元格添加红色填充色标记，便于快速识别错误

### 3. 数据对比增强
- 在生成的详细对比 Excel 文件（{sheet1}_vs_{sheet2}_detailed.xlsx）中新增 psdu_crc 列
- 支持同时显示两个版本的 psdu_crc 结果
- 对 Fail 情况进行可视化标记，提高数据审查效率

### 3. 图表绘制
- 绘制对比图表，显示 Mag Tracking 开启和关闭时每个 MCS 的 EVM 随功率变化
- 支持两种图表显示方式：
  - 所有 MCS 在一个图上（默认）：便于整体对比
  - 每个 MCS 单独一个图：便于详细分析单个 MCS 的性能
- 为每个 MCS 使用不同的颜色和标记
- 添加标题、坐标轴标签、图例和网格
- 图例显示 MCS 和空间流信息

### 4. 结果保存
- 保存图表为 PNG 文件（100 DPI，高分辨率）
- 确保输出目录存在

### 5. 统计信息
- 输出每个测试文件的简单统计信息：
  - 功率范围
  - 每个 MCS 的 EVM 范围（NSS0 和 NSS1）

## 使用方法

### 1. 直接运行
```bash
python evm_comparison.py
```

### 2. 依赖库
- pandas: 数据处理
- matplotlib: 图表绘制
- os: 文件操作

### 3. 安装依赖
```bash
pip install pandas matplotlib
```

## 测试文件信息

### 文件路径
- **Mag Tracking OFF** (enb_amp0):
  `D:\users\gxu\chip_test\chip_tx\eagletest\py_script_rls3p0_chip\Log\wifi_tx\chip3_2G_he_40m_nss2_ldpc\enb_amp0\risc_wifitx_40m_hesu_nss2_stbc0_fec_coding1_channel11_enb_amp0_2026-0311-112640.csv`

- **Mag Tracking ON** (enb_amp1):
  `D:\users\gxu\chip_test\chip_tx\eagletest\py_script_rls3p0_chip\Log\wifi_tx\chip3_2G_he_40m_nss2_ldpc\enb_amp1\risc_wifitx_40m_hesu_nss2_stbc0_fec_coding1_channel11_enb_amp1_2026-0311-103340.csv`

### 文件特点
- Wi-Fi 6 (HE) 2G 频段
- 40MHz 带宽
- NSS2（2 空间流）
- LDPC 前向纠错编码
- Channel 11
- 测试日期：2026-03-11

## 输出结果

### 图表位置
- 所有 MCS 在一个图上：`D:\users\gxu\e22_tx\260311\evm_comparison_per_mcs_combined.png`
- 每个 MCS 单独一个图：`D:\users\gxu\e22_tx\260311\evm_comparison_per_mcs_separate.pdf`
- 文件格式：
  - 合并图：PNG（100 DPI，高分辨率）
  - 单独图：PDF（高分辨率，每个图表大小为 12x8 英寸，150 DPI）

### 图表内容
- X 轴：tx_power_set (dBm)，范围 [-12, 20]
- Y 轴：EVM (dB)，范围 [-45, -18]
- 红色实线（圆形标记）：Mag Tracking OFF (NSS0)
- 橙色虚线（圆形标记）：Mag Tracking OFF (NSS1)
- 蓝色实线（方形标记）：Mag Tracking ON (NSS0)
- 青色虚线（方形标记）：Mag Tracking ON (NSS1)

## 技术说明

### EVM（误差向量幅度）
EVM 是衡量无线传输信号质量的重要指标，值越小表示信号质量越好。通常 Wi-Fi 设备在 -30 dB 以下被认为是良好的。

### Mag Tracking（幅度跟踪）
Mag Tracking 是一种射频优化技术，通过跟踪信号幅度的变化来提高信号质量。开启时通常可以提高 EVM 性能，特别是在较高功率水平下。

## 注意事项

1. 确保测试文件路径正确且文件格式符合要求
2. 文件必须包含 'tx_power_set(dBm)', 'evm_nss0' 和 'evm_nss1' 列
3. 如果图表显示异常，可能需要检查数据中的无效值（如 NaN）
4. 图表将自动保存到指定路径，并同时在屏幕上显示

## 配置选项

### 图表显示方式
脚本支持两种图表显示方式，可以通过修改 `main` 函数中的 `separate` 变量进行配置：

```python
if __name__ == "__main__":
    # 设置是否按MCS分成多个图，或者所有MCS在一个图上
    separate = False  # False表示所有MCS在一个图上，True表示每个MCS一个图
    compare_evm_per_mcs(separate)
```

**选项说明：**
- `separate = False`（默认）：所有 MCS 在一个图上
  - 便于整体对比
  - 文件保存路径：`D:\users\gxu\e22_tx\260311\evm_comparison_per_mcs_combined.png`
- `separate = True`：每个 MCS 单独一个图
  - 便于详细分析单个 MCS 的性能
  - 文件保存路径：`D:\users\gxu\e22_tx\260311\evm_comparison_per_mcs_separate.pdf`
  - 每个图表大小为 12x8 英寸（150 DPI），提供更高的分辨率
  - 保存在 PDF 文件中，便于缩放和打印

## 修改说明

如果需要对比其他文件或修改图表参数，可以修改脚本中的以下部分：

1. 文件路径：在 `mag_off_file` 和 `mag_on_file` 变量中修改
2. 图表样式：修改 plt.plot 的参数（颜色、标记、线宽等）
3. 坐标轴范围：修改 plt.xlim 和 plt.ylim 的值
4. 输出位置：修改 `output_dir` 变量
5. 图表显示方式：修改 `separate` 变量
