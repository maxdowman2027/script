# reg_query.py 脚本说明文档

## 脚本概述

reg_query.py 是一个功能强大的 E22 芯片寄存器查询工具，支持通过寄存器名称或物理地址查询详细信息。该工具具有以下特点：

1. **灵活的查询方式**：支持按寄存器名称或物理地址查询
2. **可配置的CSV路径**：支持指定自定义的CSV文件目录
3. **详细的查询结果**：提供完整的寄存器信息，包括文件路径、模块信息、位字段等
4. **多种查询模式**：支持交互式查询和命令行参数查询

## GUI界面程序

reg_query_gui.py 是基于 Tkinter 的图形用户界面版本，提供了更直观的操作方式。主要功能包括：

1. **可视化查询**：支持按名称或地址查询的可视化界面
2. **多个寄存器查询**：支持同时查询多个寄存器，查询内容可以用逗号或换行分隔
3. **结果展示**：查询结果在文本区域中以格式良好的方式显示
4. **CSV路径配置**：通过"浏览"按钮选择自定义的CSV文件目录
5. **列出可用CSV文件**：显示当前CSV目录下的所有寄存器定义文件
6. **导出功能**：支持将查询结果导出到文本文件
7. **错误处理**：友好的错误提示和异常处理机制
8. **读写命令生成**：根据查询到的寄存器地址与位域自动生成读命令/写命令（含写模板与默认值示例）
9. **批量命令集中输出**：多寄存器查询时，将所有读写命令集中到命令输出区；每行仅保留纯命令，便于直接复制执行
10. **写值配置**：GUI 中可配置“写寄存器值”（多行/多值），与查询项**按顺序一一对应**，用于批量生成写命令；可用 `,,` 或空行占位跳过某一档
11. **32bit读回解析**：可输入仿真/硬件读回的整寄存器 32bit 值（十六进制或十进制），按 CSV 位域列表拆成各**信号**的取值；多条查询时与写值相同的顺序对齐/单值广播规则

## 主要功能

### 1. 寄存器查询
- **名称查询**：通过寄存器名称查询详细信息（如 `reg_tx_prefreq_ofs_force_en`）
- **地址查询**：通过32位物理地址查询（如 `0xc30270d8`）
- **结果包含**：
  - 文件路径和名称
  - 所属模块和基地址
  - 地址偏移和完整物理地址
  - 位字段列表（信号名称、位位置、默认值）
  - 寄存器32位默认值（十六进制、十进制、二进制）

### 2. CSV路径指定
- 支持通过 `-c` 或 `--csv-dir` 参数指定自定义的CSV文件路径
- 适用于不同版本的寄存器定义文件

### 3. CSV文件管理
- 通过 `-l` 或 `--list-csv` 参数列出可用的CSV文件
- 帮助用户确认CSV文件是否正确加载

### 4. 查询模式
- 命令行直接查询：快速获取结果
- 参数化查询：支持通过脚本或其他工具调用

## 使用方法

### 1. 命令行使用

#### 查询寄存器名称
```bash
python reg_query.py reg_mcs10_ldpc_man
```

#### 查询物理地址
```bash
python reg_query.py 0xc3026cc0
```

#### 指定CSV路径
```bash
python reg_query.py -c "D:\fpga_test\imag\E22_4.0\260420\csv" reg_mcs10_ldpc_man
```

#### 列出CSV文件
```bash
python reg_query.py -l
```

#### 详细参数
```bash
python reg_query.py --help
```

### 2. GUI使用

#### 启动GUI程序
```bash
python reg_query_gui.py
```

#### GUI功能说明
- **查询方式选择**：支持按名称查询或按地址查询
- **查询内容输入**：在输入框中输入要查询的寄存器名称或地址，支持多个查询，用逗号或换行分隔
- **CSV目录配置**：可以通过"浏览"按钮选择自定义的CSV文件目录
- **查询按钮**：点击"查询"按钮执行查询
- **列出可用CSV文件**：点击此按钮显示当前CSV目录下的所有寄存器定义文件
- **结果显示**：查询结果将显示在下方的文本区域中
- **命令输出区**：集中显示本次查询的所有读/写命令，每行无额外前缀，可直接复制执行
- **导出功能**：点击"导出结果到文件"按钮可将查询结果保存为文本文件
- **命令生成**：每条查询结果会附带读写命令：
  - 读命令：`hex(test_top.mem.rdm(ADDR , MSB, LSB))`
  - 写命令模板：`test_top.mem.wrm(ADDR , MSB, LSB , <value_hex>)`
  - 写命令默认值示例：`test_top.mem.wrm(ADDR , MSB, LSB , 0x...)`
  - 若按地址查询且无法定位具体位域，默认位域为 `[31:0]`
- **写值配置**：多行框中与查询**顺序对应**（逗号或换行分隔）；仅填 **1 个**写值且有多条查询时，该值**作用于全部**查询（兼容旧版）；填多个写值时与每条查询一一对应，可用 `,,` 占位跳过某一档；多于查询项时截断并提示
- **32bit读回值(可选)**：输入整寄存器读回值后，在结果中增加「按位域解析」：对每个 `bit_fields` 信号按 `[msb:lsb]` 从 32bit 中截取并显示十六进制/十进制/（≤16 位时）二进制；按名称命中单个子域且无完整位域表时，仅解析该域；与查询顺序一一对应，仅 1 个读值且多条查询时作用于全部；超过 32 位时取低 32 位

#### 多个寄存器查询示例
支持以下查询格式：
- 逗号分隔：`reg_mcs10_ldpc_man, 0xc3026cc0, reg_tx_prefreq_ofs_force_en`
- 换行分隔：
  ```
  reg_mcs10_ldpc_man
  0xc3026cc0
  reg_tx_prefreq_ofs_force_en
  ```

#### GUI使用示例
1. 启动GUI程序
2. 选择查询方式（按名称或按地址）
3. 输入查询内容
4. 可选：修改CSV文件目录（如果需要）
5. 点击"查询"按钮
6. 查看结果区域的查询结果
7. 可选：点击"导出结果到文件"保存查询结果

## 命令行参数

| 参数 | 说明 |
|------|------|
| `query` | 要查询的寄存器名称或物理地址(如0xc30270d8) |
| `-c, --csv-dir` | CSV文件目录路径(默认使用当前目录下的csv_files) |
| `-l, --list-csv` | 列出所有可用的CSV文件 |
| `-h, --help` | 显示帮助信息 |

## 查询示例

### 示例1: 查询寄存器名称
```bash
python reg_query.py reg_mcs10_ldpc_man
```

**结果：**
```
============================================================
寄存器查询结果: reg_mcs10_ldpc_man
============================================================
所在文件: phy_txdfe_reg.csv
文件路径: D:\users\gxu\scripts\reg_query\csv_files\phy_txdfe_reg.csv
所属模块: phy_txdfe_reg
模块基地址: 0xC3026800
地址偏移: 0x04C0
完整物理地址: 0xC3026CC0
寄存器名称: MCS10_LDPC_MAN

位字段列表:
  reg_mcs10_ldpc_man                        [8:6]      = 3'd2
  reg_mcs11_ldpc_man                        [11:9]     = 3'd2
  ...

寄存器32bit默认值: 0x0C653494 (207959252 十进制)
二进制默认值: 0b00001100011001010011010010010100
============================================================
```

### 示例2: 查询物理地址
```bash
python reg_query.py 0xc3026cc0
```

**结果：**
```
============================================================
寄存器地址查询结果: 0xc3026cc0
============================================================
所在文件: phy_txdfe_reg.csv
文件路径: D:\users\gxu\scripts\reg_query\csv_files\phy_txdfe_reg.csv
所属模块: phy_txdfe_reg
模块基地址: 0xC3026800
地址偏移: 0x04C0
完整物理地址: 0xC3026CC0
寄存器名称: MCS10_LDPC_MAN

位字段列表:
  reg_mcs10_ldpc_man                        [8:6]      = 3'd2
  ...

寄存器32bit默认值: 0x0C653494 (207959252 十进制)
二进制默认值: 0b00001100011001010011010010010100
============================================================
```

### 示例3: 查询其他版本CSV文件
```bash
python reg_query.py -c "D:\fpga_test\imag\E22_4.0\260420\csv" 0xc3026cc0
```

## 文件说明

### 目录结构
```
reg_query/
├── reg_query.py              # 主查询脚本
├── base_addr.txt             # 模块基地址定义文件
├── csv_files/                # 默认CSV文件目录
│   ├── phy_common.csv        # 通用模块寄存器
│   ├── phy_txfreq.csv        # 发射频率模块寄存器
│   ├── phy_txdfe_reg.csv     # 发射DFE模块寄存器
│   ├── phy_rxfreq.csv        # 接收频率模块寄存器
│   ├── phy_rxtime.csv        # 接收时间模块寄存器
│   ├── phy_txbf.csv          # 发射BF模块寄存器
│   ├── phy_fft.csv           # FFT模块寄存器
│   └── phy_rx11b.csv         # 11b接收模块寄存器
└── reg_query.skill           # 技能说明文档
```

### base_addr.txt 文件格式
```
模块名称,基地址
phy_common,0xc3026000
phy_txfreq,0xc3026400
...
```

## 依赖库
- csv: CSV文件读取
- re: 正则表达式匹配
- os: 文件操作
- argparse: 命令行参数解析
- sys: 系统相关操作

## 错误处理

### 常见问题

1. **CSV文件未找到**
   - 确认CSV文件路径正确
   - 检查文件是否存在

2. **寄存器未找到**
   - 确认寄存器名称或地址正确
   - 检查CSV文件内容是否包含该寄存器

3. **基地址未找到**
   - 检查 base_addr.txt 文件是否包含对应模块
   - 确认模块名称拼写正确

## 技术说明

### 地址计算方式
寄存器完整物理地址 = 模块基地址 + 地址偏移

### 位字段解析
支持多种位字段格式：
- 单个位：[7]
- 位范围：[11:9]
- 默认值格式：3'd2、1'b0、8'hFF等

### CSV文件格式
CSV文件使用分号(;)分隔符，包含以下列：
- 地址偏移
- 寄存器名称
- 位位置
- 默认值
- 信号名称

## 使用建议

1. **查询前准备**：确认CSV文件路径正确，文件内容完整
2. **查询精度**：使用完整的寄存器名称可以获得更准确的结果
3. **地址格式**：物理地址支持十六进制(0x前缀)或十进制格式
4. **版本兼容性**：不同版本的寄存器定义可能存在差异，建议使用对应版本的CSV文件

## 修改说明

如果需要更新功能或修复问题，可以修改以下关键部分：

1. **基地址定义**：修改 base_addr.txt 文件添加新模块
2. **查询逻辑**：修改 find_register_info() 和 find_register_by_address() 函数
3. **CSV解析**：修改 calculate_reg_default() 函数以支持新的位字段格式
4. **输出格式**：修改 print_register_info() 函数调整输出内容
